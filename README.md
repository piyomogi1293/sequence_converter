# Sequence Converter

PNG/JPG形式のシーケンス図画像をPlantUMLテキスト形式に変換するPythonツール。

## インストール

```bash
uv pip install -e ".[dev]"
```

## 使用方法

```bash
sequence-converter convert --input input/ --output output/
```

## 開発

詳細なドキュメントは後ほど追加されます。
